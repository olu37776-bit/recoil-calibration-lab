# CURRENT · V0.2.0

日期：2026-09-23。目标仓库：`olu37776-bit/recoil-calibration-lab`，用户已建立公开仓库，保留其可见性，不上传用户媒体。

## 已实现范围

V0.1 核心源码、测试和工具保持；新增 `catalog.py`、`screenshots.py`、`ui.py`、本地 Web 界面和对应测试。目录包含14把自动射击类步枪/冲锋枪/轻机枪、49个配件、18种弹药；是人工核对的子集。枪械名不是经过游戏实测的补偿模板。

截图分为静态弹着统计、双图总位移、显式时间戳序列。只有满足 Trial 门禁的序列可进入原有训练链路；还需要独立 Response 和多轮数据。没有实现单张图生成完整压枪曲线。

配装选择含来源、解锁和费用；不明字段为 null，未知兼容默认不放行。完整 Context 指纹隔离每一种配装、倍率、设置和游戏版本。

## 验证与证据

运行 `python tools/verify.py`，以 `evidence/verification.json` 及源码清单为准，禁止使用旧计数替代新验证。原有87项测试在迁移前重新通过；新测试覆盖目录、配置、三类截图和本地 HTTP。

浏览器记录见 `evidence/browser-check.json`：受环境浏览器导航策略限制，仅做 DOM + 进程内接口桥接冒烟，HTTP 单测独立执行。不是完整网络 E2E；localStorage 持久化未验证。

Windows、真实游戏数据、硬件输入响应、驱动和独立审查均未完成。远端工作流结果以实际 GitHub Actions 为准，本地脚本不宣称远端通过。

## 下一步

1. 用户真实截图/录像验证，确保观测区域与游戏中的瞄准位移一致。
2. 将 response / fit / refine / validate 接入统一界面，保持证据与版本门禁。
3. 校对其余武器/配件、未知售价和兼容关系，再追加可选条目。
4. 最后做已授权环境的输入适配器与实时采集；默认仍 RecordingSink。
