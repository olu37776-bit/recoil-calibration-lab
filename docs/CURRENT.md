# CURRENT · V0.3.0

日期：2026-09-23。仓库：olu37776-bit/recoil-calibration-lab。保留公开可见性，不上传用户媒体。

## 本轮交付

保留V0.2的14枪械/49配件/18弹药目录、截图三模式和原校准算法。

新增state.py完整观察门禁及精确Context曲线选择；state_demo.py的16种合成场景；hud.py的固定ROI参考图候选比较；/state.html状态实验室。读docs/STATE_FEEDBACK.md了解输入契约、限制和验收范围。雷蛇鼠标核对见docs/DEVICE_SUPPORT.md。

状态门禁能消费外部完整快照，在空仓/换弹/投掷/近战/背包/腰射/治疗/失焦/未知/过期/配置变化时停止RecordingSink回放，并要求释放后重启。没有实时识别当前游戏状态。

HUD端点只是离线参考图比较，unknown/歧义/同图自检显式标记；不输出真实弹量、不自动接回放，不声称游戏识别准确率。

## 验证

每次运行python tools/verify.py，源码指纹与evidence/verification.json必须匹配。新测试与原测试一起执行。当前版本的实机游戏、驱动、全局输入采集及独立审查未完成。

已重新执行并替换浏览器/包冒烟报告：浏览器仅DOM+进程内接口桥接，安装包为本轮V0.3。不是完整浏览器网络E2E。新HEAD的远端验证以GitHub Actions实际结果为准，不由本地测试代替；上轮Windows任务最后读数仍未完成，不标记通过。

## 下一步

先取得不同真实采集会话的原始HUD截图/录像，验证识别条件、误放行和延迟；再做连续帧状态融合。继续将response/fit/refine/validate接入界面。最后适配当前鼠标驱动并重新标定输入响应。

不根据商品宣传承诺全枪全姿态自动识别、不用固定蹲姿倍率替代实测，不做随机人手/检测规避。
