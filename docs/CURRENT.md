# CURRENT · V0.4.0

日期：2026-09-23。基线main：1165782c24b860e764dbed237dc4bb73c7bd8324。仓库：olu37776-bit/recoil-calibration-lab。保留公开可见性，不上传用户媒体。

## 本轮交付

保留配装、截图三模式、离线校准、固定ROI、状态门禁。新增adaptive_hud.py的多尺度局部定位、temporal.py的连续帧候选确认、自适应页面adaptive.html/adaptive.js。

参考区域标一次，自动读取待测图尺寸并在边缘/中心锚定附近搜索；模板支持本机导入导出，换待测图可自动分析。不要求参考图与待测图同分辨率，不对宽屏整图强制拉伸。缺失/歧义返回unknown。

连续帧确认通过/api/hud-sequence离线接口提供；新标签、unknown、重复图片、尺寸变化和时钟间隔分别处理。未接实时采集和StatePlayback；无游戏预训练参考包、无自动弹量/枪械识别。参考图首次仍需标签和小区域；录像自动抽帧未完成。

HUD适配不代表校准曲线适配：不修改输入counts，不取消Context、不沿用未重验的游戏验证结论。详见AUTOMATION.md。

## 验证

本轮源文件与evidence/verification.json对齐。覆盖合成720p/1080p/1440p/4K/宽屏、独立HUD比例、局部偏移、未知/重复/歧义和序列门禁。没有游戏准确率结论。

浏览器使用Chromium DOM+进程内API桥接，真实浏览器网络导航被环境阻止；HTTP接口有独立测试。可选复现脚本tools/smoke_adaptive.py。安装包单独检查。真实游戏、Windows硬件、驱动、实时停止延迟及独立审查未完成。

上轮1165782远端Linux通过；Windows两组为cancelled（不再写仍在运行），日志获取返回404，尚未定位原因。本轮远端必须重新查新HEAD，不能沿用上轮或本地PASS。工作流增加verbose测试输出便于定位，不声称已修复Windows。

## 下一步

优先单段录像自动读取媒体时间并抽取候选片段，降低人工整理截图工作；再用真实独立会话衡量误识别。自动化校准向导、参考包增长和驱动仍按ROADMAP推进。不能根据商家宣传承诺无设置全自动。
