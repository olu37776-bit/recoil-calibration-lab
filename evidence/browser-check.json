{
  "status": "PASS",
  "version": "0.3.0",
  "scope": "DOM-only Chromium with in-process API bridge. Browser HTTP navigation blocked; HTTP independently unit-tested. No game/driver/hardware.",
  "checks": [
    "state lab bootstrap via in-process bridge; no real browser HTTP",
    "empty stops and holding after recovery waits for release",
    "pose change stops instead of silently reusing old curve",
    "uploaded two synthetic references and independent query; correct candidate, no execution permission",
    "changing HUD inputs invalidates displayed result",
    "mobile horizontal overflow=False"
  ],
  "page_errors": [],
  "browser_network_e2e": false
}
